import './styles.scss';
import { useMemo } from 'react';

const ProfileStories = () => {
  return useMemo(
    () => <div className="profile-stories-container">profile stories</div>,
    []
  );
};

export default ProfileStories;
